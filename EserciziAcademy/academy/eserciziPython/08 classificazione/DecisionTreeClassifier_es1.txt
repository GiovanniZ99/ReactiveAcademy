import matplotlib.pyplot as plt
from sklearn.datasets import load_iris
from sklearn.model_selection import train_test_split
from sklearn.tree import DecisionTreeClassifier, plot_tree
from sklearn.metrics import accuracy_score

# 1 Caricare il dataset Iris e visualizzare features e target (dati)
iris = load_iris()   # disponibile dalla libreria sklearn
X, y = iris.data, iris.target  # X: caratteristiche, y: etichette di classe

# 2 Dividere i dati di training e test

# 3 Creare e addestrare il modello Decision Tree
model = DecisionTreeClassifier(max_depth=3, random_state=42)  # max_depth limita la profondità dell’albero  ; valutare l'effetto di questi parametri

# 4 Visualizzare l'albero decisionale

# 5 Valutare l'accuratezza al variare dei parametri








